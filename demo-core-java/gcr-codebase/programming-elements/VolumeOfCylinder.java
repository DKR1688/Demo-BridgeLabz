public class VolumeOfCylinder {
    public static void main(String[] args) {
        double radius = 3.0;
        double height = 12.0;

        // This formula is to calculate volume
        double volume = Math.PI * radius * radius * height;
        System.out.println("Volume of the cylinder: " + volume);
    }
}