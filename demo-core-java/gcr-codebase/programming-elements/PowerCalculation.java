public class PowerCalculation {
    public static void main(String[] args) {
        int exponent = 6;
        int base = 3;

        // we will calculate power using Math.pow
        double ans = Math.pow(base, exponent);
        System.out.println(base + " raised to the power of " + exponent + " is: " + ans);
    }
}
